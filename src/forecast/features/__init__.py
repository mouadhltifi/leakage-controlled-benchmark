"""PLACEHOLDER (reuse-only): v3 feature package.

v3 reuses :mod:`mmfp.features` wholesale — price/TA feature computation,
news aggregation + statistics, social features, macro events, and the
audit-critical :class:`mmfp.features.scalers.FittedScaler`. See
the architecture spec reuse map.

No v3-specific feature code is planned; this placeholder exists so the
v3 import path mirrors v2's. No Python code lives here yet.
"""

from __future__ import annotations

__all__: list[str] = []
