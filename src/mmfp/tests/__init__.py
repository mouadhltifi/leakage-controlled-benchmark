"""Test package for mmfp.

Layout:

* ``unit/`` — fast, per-module tests
* ``integration/`` — end-to-end runs (Milestone 7+)
* ``reproducibility/`` — CPU bit-identical and MPS tolerance tests
* ``leakage/`` — scalers fit on train only, no future info, etc.
"""
