"""Runtime aggregation strategies (slot for future learned pooling).

Milestone 5 deliverable. Currently a placeholder — axis 3 candidates in
the current brief are all feature-build-time (see
:mod:`mmfp.features.news_aggregate`). This directory exists so a later
experiment can plug in a learned attention pooling layer without
restructuring. See spec Section 3.6.
"""
