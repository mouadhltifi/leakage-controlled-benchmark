# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Secrets — never commit
.env
.env.*
*.key
kaggle.json
.kaggle/

# OS
.DS_Store

# Generated figures (regenerate with scripts/figures/make_paper_figures.py)
figures/*.pdf

# Derived/fetched data — kept OUT of the git history to keep clones lean; the
# derived-feature deposit ships alongside the repository (see data/README.md for
# the build order and DATA-STATEMENTS.md for per-source terms) and is
# DOI-archived on acceptance. The committed result CSVs in results/ already
# reproduce every headline number and figure.
data/external/
data/interim/
data/raw/prices/
data/processed/
