# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Secrets — never commit
.env
.env.*
*.key
kaggle.json
.kaggle/

# OS
.DS_Store

# Generated figures (regenerate with scripts/figures/make_paper_figures.py)
figures/*.pdf

# Raw/intermediate data — not shipped. Raw prices and raw third-party text are
# not redistributable (see DATA-STATEMENTS.md); intermediates are regenerable.
# The derived-feature deposit data/processed/ IS committed (raw-text-free
# layer, per-source terms preserved; covered by MANIFEST.sha256) and is
# DOI-archived on Zenodo (10.5281/zenodo.21431362); see data/README.md.
data/external/
data/interim/
data/raw/prices/
